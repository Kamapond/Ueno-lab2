# Copy this file to `.env` and fill in your own values. NEVER commit the real `.env`.
# (`.env` is git-ignored; only this example file is tracked.)

# OpenAI (LLM answering + text-embedding-3-small)
OPENAI_API_KEY=

# Neo4j (AuraDB) connection for the GraphRAG knowledge graph
NEO4J_URI=
NEO4J_USERNAME=
NEO4J_PASSWORD=
